import mutations from "./conversation.mutations.typedefs";
import queries from "./conversation.queries.typedefs";
import types from "./conversation.types.typedefs";

const typeDefs = [types, queries, mutations];

export default typeDefs;