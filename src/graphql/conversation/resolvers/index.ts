import Mutation from "./conversation.mutations.resolvers";
import Query from "./conversation.queries.resolvers";
import Subscription from "./conversation.subscriptions.resolvers";

export default {
    Mutation,
    Query,
    Subscription,
};
