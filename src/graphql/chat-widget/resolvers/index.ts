import Mutation from "./chat-widget.mutations.resolvers";
import Query from "./chat-widget.queries.resolvers";
import Subscription from "./chat-widget.subscriptions.resolvers";

export default {
    Mutation,
    Query,
    Subscription,
};
