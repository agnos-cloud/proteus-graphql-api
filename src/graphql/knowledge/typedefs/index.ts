import mutations from "./knowledge.mutations.typedefs";
import queries from "./knowledge.queries.typedefs";
import types from "./knowledge.types.typedefs";
import subscriptions from "./knowledge.subscriptions.typedefs";

const typeDefs = [types, queries, mutations, subscriptions];

export default typeDefs;
