import Mutation from "./knowledge.mutations.resolvers";
import Query from "./knowledge.queries.resolvers";
import Subscription from "./knowledge.subscriptions.resolvers";

export default {
    Mutation,
    Query,
    Subscription,
};
