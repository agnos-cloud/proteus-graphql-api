import Mutation from "./user.mutations.resolvers";
import Query from "./user.queries.resolvers";
import Subscription from "./user.subscriptions.resolvers";

export default {
    Mutation,
    Query,
    // Subscription,
};
