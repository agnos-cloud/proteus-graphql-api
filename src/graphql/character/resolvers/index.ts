import Mutation from "./character.mutations.resolvers";
import Query from "./character.queries.resolvers";
import Subscription from "./character.subscriptions.resolvers";

export default {
    Mutation,
    Query,
    Subscription,
};
