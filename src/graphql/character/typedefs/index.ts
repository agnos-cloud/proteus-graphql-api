import mutations from "./character.mutations.typedefs";
import queries from "./character.queries.typedefs";
import types from "./character.types.typedefs";
import subscriptions from "./character.subscriptions.typedefs";

const typeDefs = [types, queries, mutations, subscriptions];

export default typeDefs;
