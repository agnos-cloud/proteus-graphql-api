import Mutation from "./message.mutations.resolvers";
import Query from "./message.queries.resolvers";
import Subscription from "./message.subscriptions.resolvers";

export default {
    Mutation,
    Query,
    // Subscription,
};
