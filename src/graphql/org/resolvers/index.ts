import Mutation from "./org.mutations.resolvers";
import Query from "./org.queries.resolvers";
import Subscription from "./org.subscriptions.resolvers";

export default {
    Mutation,
    Query,
    // Subscription,
};
