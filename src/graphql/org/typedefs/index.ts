import mutations from "./org.mutations.typedefs";
import queries from "./org.queries.typedefs";
import types from "./org.types.typedefs";

const typeDefs = [types, queries, mutations];

export default typeDefs;